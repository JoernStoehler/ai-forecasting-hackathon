# Run Checks
<!-- REVIEW: add/remove commands if you want stricter coverage. -->
- From repo root (monorepo commands):
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - `npm run check` (lint + typecheck + build)
- `npm run dev` (root) proxies to the webapp workspace dev server.
