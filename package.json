{
  "name": "ai-forecasting-hackathon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/webapp",
    "build": "npm run build -w packages/engine && npm run build -w packages/cli && npm run build -w packages/webapp",
    "lint": "npm run lint -w packages/engine && npm run lint -w packages/cli && npm run lint -w packages/webapp",
    "typecheck": "npm run typecheck -w packages/engine && npm run build -w packages/engine && npm run typecheck -w packages/cli && npm run typecheck -w packages/webapp",
    "check": "npm run lint && npm run typecheck && npm run build",
    "hello": "bash scripts/hello.sh"
  }
}
