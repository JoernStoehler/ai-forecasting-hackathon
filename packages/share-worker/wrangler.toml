name = "share-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# KV namespace for storing shared game states
[[kv_namespaces]]
binding = "SHARES"
id = "4f0b3d10f63640708e2574bcf2bf4a09"
preview_id = "fe5f2e79b8ba49b9a8eaf5705c5cabc2"

# CORS: Worker allows all origins since it only stores/retrieves game state (no sensitive data)
# This enables the game to work from any hosting environment (AI Studio, artifacts, localhost, etc.)
