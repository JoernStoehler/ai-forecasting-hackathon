name = "share-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# KV namespace for storing shared game states
[[kv_namespaces]]
binding = "SHARES"
id = "placeholder-production-id"
preview_id = "placeholder-preview-id"

[vars]
# CORS origins allowed to access this worker
# Update these when deploying to production
ALLOWED_ORIGINS = "http://localhost:3000,http://localhost:3001,http://localhost:4173,http://localhost:5173"
