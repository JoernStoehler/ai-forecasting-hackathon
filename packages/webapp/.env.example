# Development only. Do not commit real keys.
GEMINI_API_KEY=

# Set to 'true' to use mock forecaster instead of real Gemini API (useful for testing/development)
# When enabled, GM turns return deterministic placeholder events without requiring an API key
VITE_USE_MOCK_FORECASTER=false

# Share worker URL (defaults to localhost:8787 for local development)
# Set this to your deployed Cloudflare Worker URL in production
VITE_SHARE_WORKER_URL=
